<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Saferoute">

        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.Saferoute">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:theme="@style/Theme.Saferoute"/>
        <activity android:name=".FakeCallActivity" android:theme="@style/Theme.Saferoute"/>
        
        <!-- The main contacts menu and the three new screens -->
        <activity android:name=".TrustedContactsActivity"/>
        <activity android:name=".YourInformationActivity"/>
        <activity android:name=".EmergencyContactsActivity"/>
        <activity android:name=".FakeCallerSettingsActivity"/>
        <activity android:name=".AppGuideActivity"/>

        <service
            android:name=".SOSService"
            android:foregroundServiceType="location" />
        <service 
            android:name=".ShakeDetectorService"
            android:foregroundServiceType="dataSync"/>
        <service android:name=".SirenService" />
        <service android:name=".LiveLocationService"/>

    </application>
</manifest>